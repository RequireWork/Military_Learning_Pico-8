x = 30
y = 30
function _draw()
	cls(1)
	spr(1,x,y)
	if (btn(0)) x -= 2
	if (btn(1)) x += 2
	if (btn(⬆️)) y -= 2
	if (btn(⬇️)) y += 2
	
end
//First Code To Test Out On Pico!

print("hello world")
rectfill(80,80,120,100,12)
circfill(70,90,20,14)
for i=1,4 do print(i) end
